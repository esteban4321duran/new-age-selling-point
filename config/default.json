{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "tiny",
    "session": {
      "store": "@foal/typeorm",
      "csrf": {
        "enabled": true
      }
    }
  },
  "database": {
    "type": "better-sqlite3",
    "database": "./db.sqlite3"
  }
}